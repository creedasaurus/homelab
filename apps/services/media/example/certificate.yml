apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cert
spec:
  secretName: example-tls
  dnsNames:
    - "example.local.vulf.haus"
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
